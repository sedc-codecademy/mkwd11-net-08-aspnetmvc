@model List<News>

@{
    if (Model.Count == 0)
    {
        <p>No news based on your search</p>
    }
    else
    {
        <ul>
            @foreach (var news in Model)
            {
                <li>@news.Title [@news.Text]</li>
            }
        </ul>
    }
}