@model OrderDetailsViewModel

<h5>The user @Model.UserFullName ordered pizza with <label asp-for="PizzaName"></label> @Html.DisplayFor(viewModel => viewModel.PizzaName)</h5>

<p>The price of the order is @Model.Price</p>